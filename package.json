{
  "name": "hkl-sop-kks",
  "version": "2.0.0",
  "description": "HKL Standards — modulare Single-Page-App (public/) + zero-dependency Persistenz-Server (server/)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node scripts/check.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED",
  "private": true
}
